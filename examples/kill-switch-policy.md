# Kill-Switch Policy Example

This bundle includes a normal policy and a targeted kill-switch for emergency blocking.

Use when you need immediate containment for a known bad segment without waiting for new code rollout.
